import pure from 'omniscient';

const Position = pure(
  ({ local, remote }) => <div>
    Position

    calibration status
    calibration values

    reset magnotemeter
    reset accel/gyro
  </div>
);

export default Position;
